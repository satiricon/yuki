services:
  yuki.file.commands:
    class: \Drupal\yuki\Commands\FileCommands
    tags:
      - { name: drush.command }
    calls:
      - method: setSaverChain
        arguments:
          - "@yuki.saver_chain"